gomag:
  login:
    username_selector: 'input[name="username"], input[type="email"], input[name="email"]'
    password_selector: 'input[name="password"], input[type="password"]'
    submit_selector: 'button[type="submit"], input[type="submit"]'
    post_login_wait: 2.5

  categories:
    # URL corect confirmat de tine:
    url_path: "/gomag/product/category/list"
    # selector simplu; fallback-ul parseaza oricum tabelul daca nu iese
    item_selector: "table tbody tr td:first-child"

  import:
    url_path: "/gomag/dashboard/products/import"
    file_input_selector: 'input[type="file"]'
    start_import_selector: 'button:has-text("Import"), button:has-text("Start"), button[type="submit"]'
